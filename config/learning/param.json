{
    "shuffle_buffer_size" : 10000,
    "batch_size" : 128,
    "training" : {
        "optimizer" : "adam",
        "metrics" : ["accuracy"],
        "loss" : "categorical_crossentropy",
        "epochs" : 10,
        "steps_per_epoch": 0
    }
}